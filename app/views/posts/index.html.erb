<% @posts.each do |post| %>
<%=  post %> <%= button_to "Respond", new_response_path, params: {'post_id' => post.id}, method:'get'%>
<% end %>

<%= button_to "Home", vendor_path(current_vendor), method: 'get' %>
