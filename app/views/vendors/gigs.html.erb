<h1>Your Gigs</h1>
<% @gigs.each do |gig| %>
<!-- <p>User: <%= gig.user.username %></p>
<p>Title: <%= gig.title %></p>
<p>Description <%= gig.description %></p> -->

<div class="card">
  <div class="card-image">
    <img src="images/sample-1.jpg">
    <span class="card-title"><%= gig.user.username %></span>
    <a class="btn-floating halfway-fab waves-effect waves-light red"><i class="material-icons">add</i></a>
  </div>
  <div class="card-content">
    <p><%= gig.title %></p>
  </div>
</div>

 <%= button_to "Go To This Post", message_path(gig), method: 'get' %>
<!-- add button that allows user and vendor to go to show page of message controller and then allows to message -->
<% end %>
